<% title = t('common.new',model_name: Student.model_name.human) %>

<%= render partial: 'shared/display_title_btns', locals: {title: title, back: true} %>

<%= simple_form_for @student, wrapper: :horizontal_form, html: {  autocomplete: "off", :class => 'form-horizontal custom-form' } do |f| %>
	<%= render partial: 'form', locals: {f: f} %>
<% end %>